name: Test react app
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get reposetory code 
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
      - name: test app
        run: npm run test 